# Memoria — 2026-09-13

## Revisión tarea 21.5 (lane bloques 6-9) — veredicto RECHAZADO

Repo revisado: summonaikit-claude (`/Users/dn/dev/summonaikit-claude`, Mac). Rama `work/bloque9-21.5`, PR #320 a master. `<!-- project: github.com/gon0801/goncloud-openclaw -->`

**Defecto principal (corrección):** el preflight reporta PASS cuando el Stop bloquea, para `TARGET=cursor`. El veredicto se extrae re-parseando stdout y solo reconoce `"decision":"block"` o `"continue":false`; `cursor` bloquea por otro canal (`{"followup_message":...}` con exit 0). `<!-- project: github.com/gon0801/goncloud-openclaw -->`

- Puntos exactos: `hooks/summonaikit-harness.sh:4619-4623` (extracción del veredicto del preflight); emisores de bloqueo `emit_gate_failure` en `:3706-3708` y `emit_budget_exhausted` en `:3766-3768`; `codex`/`dsh` sí entran por `decision:block` (`:3725`, `:3784`).
- Medido: con recibo roto, `cursor STOP rc=0` vs `cursor PREFLIGHT rc=0 PASS` (diverge); `claude STOP rc=2` vs `claude PREFLIGHT rc=1 FAIL` (ok). Lo mismo con presupuesto agotado (`cycle=5`).
- Gap #2: el corpus G9 solo ejerce `claude` — `tests/lib/preflight_cases.sh:25,28,116,132,138,160,163` hardcodean `lab_run … claude`; `CASOS_G9` en `:174` no tiene ningún caso `cursor`. Por eso el defecto #1 es invisible.
- Gap #3 (documental): `Plans.md:249` y `.saikit/decisiones/21.5.tsv` afirman "mismo parser y mismas reglas … mismo veredicto" sin acotar target; para `cursor` es falso.
- `decisiones`: ruta `.saikit/decisiones/21.5.tsv`. Veredicto sellado NO se escribió (el despacho no lo pidió; no hay `.saikit/veredictos/` nombrado). `<!-- project: github.com/gon0801/goncloud-openclaw -->`

**Puntos atacados que dieron OK:** (a) el no-op de `adv_limpiar_zona` es correcto y no deja nada sin limpiar — enumeré las escrituras alcanzables desde `stop_gate`, todas caen en el scratch; `verdict_ensure_gitignore` y `adv_asegurar_zona` no se llaman desde `stop_gate`. (b) la costura `SAIKIT_MUT_PREFLIGHT_DIVERGE` (`:4591-4597`) no es alcanzable desde producción (fase `preflight` no cableada, nadie la fija fuera de `tests/lib/preflight_cases.sh:163`) — observación diferida: vive en el hook de producción, no en `tests/`. (e) el resto de Plans/ledger sí describe lo que el código hace.

**Punto (d) — deriva de evidencia:** en el head del despacho (`4438300`) los 4 `bateria-mutations-sh{1..4}-4.log` estaban truncados a mitad de corrida (37/39/37/35 líneas, sin `test_gate_mutations: OK`), contradiciendo el "186/186" del ledger. En `b946a33` corregido (145/145/142/142, `OK` en los 4, 47+47+46+46=186). `<!-- project: github.com/gon0801/goncloud-openclaw -->`

**Blast `.saikit/findings/blast-21.5.json`:** forma válida (`nivel=4` int, con `comando`). Adjudicado Noted/verificado: el hecho (el preflight no cambia veredicto del Stop ni el estado) es cierto; lo que no cubre es la divergencia del *reporte* por target.

## Hechos de entorno durables (no específicos de repo)

- **Dos discos:** `read`/`write`/`edit`/`ls` hablan con el filesystem de Windows del gateway (`C:\Users\ehven\...`). `exec` corre en la Mac (nodo `David's MacBook Pro`, `/Users/dn/...`). Un archivo visto con `read` no aparece en `exec ls`.
- **`gh` no está en el PATH del nodo:** vive en `/opt/homebrew/bin/gh`. Usar ruta completa (`gh pr checks …`).
- **El bash del sistema en esa Mac es 3.2** y NO parsea `hooks/summonaikit-harness.sh` (falla igual en master: es rojo preexistente, no del cambio). Para tests hay que usar brew bash 5.3 (`/opt/homebrew/bin/bash`).
- **El banco/lab del hook no monta sin `SAIKIT_HOOK_VIVO`.** Correr con `SAIKIT_HOOK_VIVO=$PWD/hooks/summonaikit-harness.sh bash tests/test_preflight_21_5.sh`.
- **`timeout` no existe en esa Mac** (es utilidad GNU); no usarlo en exec.
- **El HEAD puede moverse durante la revisión:** el implementador pusheó `b946a33` mientras yo revisaba `4438300`. Re-verificar `git rev-parse HEAD` antes de sellar cualquier juicio.
- **No sellar veredicto si el despacho no lo pide** — un artefacto no solicitado deja hash espurio.
