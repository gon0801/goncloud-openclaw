# Memoria — 2026-09-17 (reviewer / Lupa)

## Fase 7 carril P — revisión PR #63 (tablero-runbook) <!-- project: github.com/gon0801/goncloud-openclaw -->

- Repo `goncloud-openclaw`, PR borrador #63, base `main`, head `fase7/tablero` @ `45df0871ae298971deb62802b24dce67144f6b4a`. Este head invalidó mi revisión anterior (era sobre `c30de92`): en cada re-despacho con head nuevo, re-revisar completo.
- Puntos de partida medidos contra API/GitHub, no contra el clon: `origin/main` = `d4980274a75f2418c6924a5961509f7997f89a24`; `merge-base` = `e6c46d085fa68738a755b473b3d4636f2aa784eb`; `merge-tree --write-tree` = `85eae906`; `diff --diff-filter=D` entre main y el merge-tree: vacío (no hay borrados).
- Veredicto emitido: **APROBADO con observaciones**, 4 hallazgos nuevos (2 de ellos más bien deuda de main / limpieza). No escribí veredicto sellado: el despacho no lo pidió ni nombró ruta.
- Evidencia en `.saikit/scratch/P/` dentro de `/Users/dn/dev/wt-f7-P` (untracked). El worktree es solo lectura para el resto; declaré el incidente de haber escrito ahí.

### Hechos durables del pipeline (aplican más allá de este PR) <!-- project: github.com/gon0801/goncloud-openclaw -->

- **La CLI gateway NO acepta `--params @archivo.json`.** Medido: `gateway call health --params @/tmp/params-ok.json` (archivo existente, JSON válido) → `Gateway call failed: --params must be valid JSON.` EXIT=1. La forma que funciona es `--params "$(cat archivo.json)"` → EXIT=0. Cualquier doc, DoD o runbook que enseñe la forma con `@` está roto y quien lo corra al pie de la letra falla.
- Lugares en `main` que aún enseñan la forma rota (7, lista completa): `docs/spec/runbook-progress.v1.md:11`, `docs/runbooks/autopilot-fase7.md:449` y `:462`, `docs/runbooks/autopilot-fase6.md:64`, `docs/runbooks/loop-autopilot.md:142`, `Plans.md:269` y `Plans.md:273`.
- Los peligrosos para el canario de 7.6: `Plans.md:273` (fila de la DoD) y `docs/runbooks/autopilot-fase7.md:462` (paso del cierre Q3). Residuo (nadie los corre en esta fase): el resto.
- **`.gitignore` del repo usa `tablero-runbook/*.jsonl` y `*.log`, que solo cubren nivel 1.** El layout real que escribe el plugin es `<stateDir>/events/<fase>.jsonl` (+ rotación `<fase>.jsonl.1.jsonl`). Prueba: `git check-ignore -q --no-index tablero-runbook/data/events/6.jsonl` → NO ignorado. Lo que hoy salva al repo es que el stateDir default vive fuera del clon (si no, el estado se trackea). Fix correcto: `tablero-runbook/**/*.jsonl` y `**/*.log`, más una ruta anidada en la sonda `scripts/tests/test-salida-del-observador-no-trackeada.sh` (hoy solo prueba nombres de nivel 1).
- `fusionarEventos` / `EVENTOS_TOPE` / `claveEvento` (`tablero-runbook/lib.ts:641-651`) quedaron sin llamador de producción al sacar el import en `index.ts:119-121`, pero la DoD de 7.3 (`Plans.md:270`, `autopilot-fase7.md:291`) los exige: la salida limpia es corregir el comentario, no borrar la función.
- La rama `{doc:{...}}` en `tablero-runbook/index.ts:169-174` no tiene tests y su comentario se justifica con la forma `--params @fixture.json` (la rota).
- O2 del verificador (docs con la forma rota) = deuda preexistente de `main`, no del PR: ninguno de esos archivos está en el diff del PR.
- O1 del verificador (clon viejo `00f49e5` vs `d4980274`) = artefacto de su entorno, no hallazgo contra el PR. Se desmiente con fetch fresco (`git ls-remote` + API + refs locales coincidiendo).
- Los tests nuevos del PR discriminan: `tablero-runbook/index.test.ts:247` (1000 previos + 2 nuevos ⇒ 1002 líneas) e `index.test.ts:238` (`%zz` ⇒ 400 en las dos rutas). `index.test.ts` pasa de 16 a 17 casos (+1).
- La ruta completa del verificador por si hace falta la lista sin truncar: `agent:verifier:dashboard:7c6c476b-5826-404d-8e25-2ec0df76b335`.

### Reglas de operación confirmadas en este turno <!-- project: github.com/gon0801/goncloud-openclaw -->

- Dos máquinas: `read`/`write`/`edit`/`ls` = disco Windows del gateway (`C:\Users\ehven\...`); `exec` = Mac (`/Users/dn/...`). Un archivo de uno no existe en el otro. No levantar servidores HTTP para pasar archivos: pedir al lead que los mueva.
- `sessions_search` y `sessions.list` por RPC fallan con `unable to open database file` sin ámbito/params. Pasar siempre `agentId` + `sessionKeys`, o cualquier param (ej. `{"limit":1}`).
- La batería completa se corre UNA vez por tarea y el SHA validado por CI cuenta como esa corrida: no re-correr localmente, verificar el delta estáticamente (conteo de casos) y revisar el diff.
- Un turno donde el despacho no nombra blast ni veredicto sellado: no adjudico blast y no escribo veredicto. No inventar rutas de `.saikit/`.
- Worktrees de la fase: `/Users/dn/dev/wt-f7-D` y sesión tmux `fase8-lead` son de Hermes — no tocar. `/Users/dn/dev/wt-f7-P` es solo lectura.
