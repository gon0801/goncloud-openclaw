1. ALTA: `pkill -f "tg_mock.py --mode"` en `trap` y `stop_mock` mata cualquier mock del box (T4 u otro T2 en paralelo) — flaky y falsos rojos.
2. ALTA: `start_mock` no guarda PID ni chequea que salio `MOCK_PORT`; si el mock no arranca, `MOCK_PORT` queda vacio/basura y el SUT falla por conexion, no por el fix.
3. MEDIA: `MOCK_PORT` se lee de stdout+stderr juntos (`> mock.port 2>&1`) con `sed` a todo el archivo — un warning de Python ensucia el puerto.
4. MEDIA: `[ "$NPOST" = 2 ]` / `= 0` es igualdad de string; `wc -l` con espacios (BSD/mac, a veces GNU) da falso rojo en casos 1/3/5.
5. MEDIA: legacy no pasa el `.txt` al SUT — si el viejo exige argv, falla por uso, no por destinos. VERIFICAR: el viejo de verdad ignora el archivo.
6. MEDIA: el parche legacy es un `sed` exacto de `https://api.telegram.org` y `source /home/claw/.secrets/telegram-sales.env` — si el viejo usa `$HOME`, otra URL o host variable, no llega al mock. VERIFICAR: esas cadenas existen tal cual.
7. MEDIA: caso 1 es el unico con poder discriminante de #4 (sin fix: `22222 33333` o 3 posts, no `11111 22222`); casos 2-6 no cubren destinos.
8. MEDIA: caso 2 solo pide `DIGEST_PARCIAL` y `rc=1` — pasa si no mando el primero, si no hay 2 POSTs, o si el texto sale en un fallo distinto.
9. BAJA: caso 3 y 5 no exigen `rc!=0`; un SUT que imprime el ERR y sale 0 igual PASS.
10. BAJA: `sleep 0.3` post-`pkill` no espera la muerte; el mock siguiente puede convivir un rato (puerto distinto, ruido en logs).
11. BAJA: mock siempre HTTP 200 con `ok:false`. VERIFICAR: el SUT no usa `curl -f` / status HTTP, si no caso 2 no discrimina.
12. BAJA: mock parsea `application/x-www-form-urlencoded`, no JSON body. VERIFICAR: el sender manda form; si manda JSON, caso 1 explota con `chat_id=None` (falso rojo).

Poder discriminante ok donde importa: caso 1 caza #4; 3 caza envio sin tope 4096; 4 caza CR; 5 caza vacio; 6 caza parser que solo acepta `"ok":true` compacto. El mock (puerto 0, bind antes de imprimir, `fail-second`/`fail-third`, compact/spaced, `message_id` incremental, flush por POST) esta bien para lo pedido.
