# Autopilot de la Fase 11 — corrida diaria, pantalla y revisión independiente (Orbit)

Esto lo ejecutas tú, el **lead**, en autopilot; David no está y no se le pregunta nada. **Hereda `docs/runbooks/base-orbit.md` v1.1** (roles, lanzamiento, precondiciones comunes, reglas de trabajo, compuertas comunes, cierre y atores de Orbit): nada de eso se repite aquí. Plan: `plans/repricing-01.md` v1.3 en `origin/master` de Orbit; sus filas mandan en la DoD.

La fase deja el motor de precios **listo para la sombra**: la corrida diaria (A.5), la pantalla y los avisos (A.6), la revisión independiente (R.1) y, solo si la Fase 10 dejó veredicto completo, la regla del envío en la ingesta (E.0b). **No aplica migraciones, no instala el cron en el servidor, no siembra claves ni goals, no despliega y no mueve ningún precio**: eso es D.0, D.1 y A.4, de David. Versión 1.1, 2026-09-18 UTC: borrador de 15 minutos más una ronda única (un lector fresco que ejecutó, 14 ítems; un revisor, 10 ítems; todos aplicados). Residuales declarados en el PR: los archivos que las filas A.5, A.6 y E.0b del plan no listan y esta fase sí (ver Inventario).

## Precondiciones propias (después del 0.0 del base, con `<N>` = 11)

```
/opt/homebrew/bin/git -C /Users/dn/dev/goncloud-Orbit cat-file -e origin/master:app/precio/goals_write.py && echo 1 || echo 0
/opt/homebrew/bin/git -C /Users/dn/dev/goncloud-Orbit cat-file -e origin/master:app/precio/cobertura.py && echo 1 || echo 0
/opt/homebrew/bin/git -C /Users/dn/dev/goncloud-Orbit show origin/master:plans/repricing-01.md | grep -cE '^\| (A\.1|A\.7) \|.*cc:完了'
/opt/homebrew/bin/git -C /Users/dn/dev/goncloud-Orbit show origin/master:plans/repricing-01.md | grep -E '^\| E\.0a \|' | grep -oE 'cc:(完了|TODO)'
/opt/homebrew/bin/git -C /Users/dn/dev/goncloud-openclaw show origin/main:docs/runbooks/base-orbit.md | head -1
```

Esperado: `1`; `1`; `2`; `cc:完了` **o** `cc:TODO`; `# Runbook base de Orbit — lo que no cambia entre fases` (distinto → `ATORADO base fuera de main`, la fase para). Las dos primeras en `0` o la tercera distinta de `2` significan que la Fase 10 no cerró: `ATORADO Fase 10 sin cerrar` y la fase para. La cuarta decide el carril D: `cc:完了` → D corre; `cc:TODO` → D **no existe en esta fase** y se declara en el progreso y en el PR de cierre. La última migración de `master` es `0039_precio.sql` y **esta fase no crea migraciones**. El worktree del lead nace con `-b fase11/revision-r1`; la base manual `orbit_fase11_manual` (regla 7 del base) se carga con **todas** las migraciones: `for m in $(ls migrations/*.sql | sort); do … -f $m; done` (la corrida toca `ads_optimizer_lock`, `ingest_run`, ledger y listings, no solo las del motor). Tracker: al arrancar y al cerrar, la tarea `AUTO-07 Repricing (plan formal)` con la skill `appflowy-ehv-task` (nota con A.5, A.6, R.1, E.0b); si el host la niega, fila del base.

## Preaprobaciones del dueño

| Operación | Alcance | Decisión |
|---|---|---|
| Arrancar la Fase 11 con las filas A.5, A.6, R.1 y E.0b (condicional) | Esta fase | **Aprobado** (esta fila es la autorización; antecedente: «ok entonces haz runbook fase 11», 2026-09-18) |
| Ramas, worktree y PRs `fase11/*` en Orbit, y mergearlos por la ruta del kit con `APPROVE lead <sha>` y CI verde | Los de este runbook, más `fase11/<carril>-cierre-r<K>` | **Aprobado** |
| Agregar `corrida.py` y `cuota.py` a la lista de excepciones del candado de pureza, cada uno con candado propio | Solo esos dos | **Aprobado** |
| Leer producción por `ssh goncloud`, solo `SELECT`, solo el lead | Readback de E.0b (si corre) | **Aprobado** |
| Editar celdas de estado de A.5, A.6, R.1, E.0b y `docs/CHAT-CONTEXT.md` | Solo en el cierre | **Aprobado** |
| Instalar el cron `10 13 * * *` en el servidor, aplicar la 0039, sembrar claves o goals, desplegar, escribir en Amazon o en la base de producción | — | **Negado**: D.0, D.1 y A.4 son de David |
| Tocar `app/spapi/precio_write.py` para sellar la forma del parche, `app/apply.py`, `app/cycle.py` o las cuotas de Ads | — | **Negado** |
| Implementar cualquier otra fila (E.3+, 0.x, B.x, M.x) | — | **Negado** |

> **Prohibido**, además de lo del base: lanzar el carril D sin el `cc:完了` de E.0a; reintentos en ráfaga contra Amazon en los tests (cliente falso siempre); un `UPDATE` sobre ledgers en E.0b; que `corrida.py` importe `app.apply`.

## Carriles

```
muse (checkout principal):   A ─────► B ─────► D (solo si E.0a cerró)
lead (wt-fase11-lead):                              R ─────► cierre
cola:                        Q1(A)  Q2(B)  Q3(D)  Q4(R)  Q5(cierre)
```

Todas las ramas nacen de `origin/master`. B arranca con el `APPROVE lead` de A; D con el de B; R cuando Q2 (y Q3 si existe) esté en `master`. `tests/test_architecture.py` lo toca A (las dos excepciones) y B no lo toca.

**A · Corrida diaria** — rama `fase11/precio-corrida` · muse. Fila **A.5** del plan, verbatim en el encargo. Además: (a) `cambiar_precio` gana el parámetro `limitador` que hoy solo tiene `revertir` (salvedad A.3 v1.3) y la corrida pasa un solo cubo de Pricing por plataforma; (b) `repartir_cupo` se llama **una vez por plataforma**, nunca con dos plataformas mezcladas (comparten moneda MX/MeLi); (c) en `shadow` los cambios virtuales nacen `confirmado/virtual` (trigger `precio_cambio_nacimiento`) para que el freno tras subida (regla #6 de S4) también viva en sombra; (d) una fila `pendiente` huérfana (COMMIT sin PATCH) se detecta al arrancar y se cierra a `error` con `error_code = 'huerfana_sin_patch'` (la 0039 exige `error_code` en `error`; no hay columna «motivo»), sin GET previo (un `error` no afirma que el precio no se movió; `revertir` cubre ese caso), queda en el resumen de la corrida y en el log, y el aviso lo da `notifica_precio` en el carril B, que agrega `huerfana_sin_patch` a sus tipos por plataforma; A puede reescribir el docstring de `cambiar_precio` (l.578–586) que hoy dice que la cierra el dueño a mano, y lo declara; (e) `L` sigue siendo el importe del escenario (el canal es de E.3/E.4); (f) las claves de las reglas se leen con `leer_config`; `precio_cap_<plataforma>` (0–20) y `precio_freno_dias_error` (1–14) se validan en `cuota.py` y `corrida.py` **replicando** la forma de `_entero` de `config.py` (duplicación deliberada: `config.py` no se toca en esta fase y `_entero` es privado), `ValueError` si faltan; **ninguna clave fuera de la tabla de umbrales del plan**, porque D.0 siembra esas 20 y nada más; los tests siembran `config_version` con `_config_version` de `tests/test_api.py`; (g) la línea de crontab va en `docs/DEPLOY.md`, sección «Crons de Orbit (crontab de `gon`, ADITIVO)», **como documentación** y pinzada por test con el patrón de `LINEA_CRONTAB_VIGILANTE` (`tests/test_spapi_vigilante.py` l.419) y de `tests/test_compose_deploy.py` l.172; instalarla es D.1; (h) A agrega **dos** excepciones al candado de pureza (`corrida.py`, `cuota.py`), como pide la fila A.5; la regla 5 del base se corrige en este mismo PR para admitirlo. **Lo que un usuario vería**: desde el contenedor app, `python -m app.cli precio --platform amazon_mx` imprime una línea por goal vigente con su resultado y motivo y termina en `decisiones=N escritas=0` en sombra; en esta fase lo ve el test con la base local y el lead en la auditoría.

**B · Pantalla y avisos** — rama `fase11/precio-pantalla` · muse. Fila **A.6** del plan, verbatim. Además: un solo sender `notifica_precio(tipo, …)` en `app/notifica.py` con el patrón de flanco por racha de los `notifica_*` existentes, que no se tocan; el recuadro de cobertura de `/precios` se calcula **en vivo** con `app/precio/cobertura.py` y `fuentes.py` (A.7), sin duplicar consultas y sin que la corrida lo persista (así se cumple «cobertura que cuadra en cada corrida» de «Diseño»); la página `/precios` vive en `app/ui.py` y su JSON en `app/api_dashboard.py` (`/api/dashboard/precios`); el bloque `plataformas.<p>.precios` va en `/api/dashboard/salud` (`app/api_dashboard.py` l.887) sin cambiar ninguna clave existente. **Lo que un usuario vería**: David abre `/precios` en el dashboard y ve los cinco bloques de S7 con la cobertura arriba; hoy todo en `sin_goal` o `fuera_de_alcance`.

**D · Regla del envío en la ingesta** — rama `fase11/envio-regla` · muse · **solo con E.0a en `cc:完了`**. Fila **E.0b** del plan, verbatim, con la tabla `fuente → cuenta / descarta / otros` de `docs/evidencia/repricing-01/E.0/veredicto.md` pegada en el encargo. La ingesta del ledger vive en `app/ledger.py` (`MAPA_KIND`, `_lookup_kind`, `mapear_destino`, `plan_eventos`, el conteo de descartes); **residual declarado**: la tabla de archivos del plan dice `app/ingest*.py`, que en este repo no existe. Muse deja en `docs/evidencia/repricing-01/E.0/consultas/` la consulta del «antes» y una `readback-despues-*.sql` de solo lectura que aplica la tabla de E.0a sobre `ledger_event` (el código de la rama no está desplegado, así que el «después» es esa consulta, no una corrida); el lead las corre en producción por la regla 6 del base y deja los dos totales de US a 90 días y la diferencia fila por fila en `docs/evidencia/repricing-01/E.0/readback.md`, en el mismo PR. **Lo que un usuario vería**: en `ingest_run` del día siguiente, los descartes de `shipping_fee` con plataforma y `fee_type`; en esta fase, el test y el readback.

**R · Revisión independiente** — rama `fase11/revision-r1` · lead, con `git merge origin/master` en esa rama cuando Q2 (y Q3) estén dentro. Fila **R.1** del plan, en dos partes. (1) **Cruzada de kimi por SHA**: `cross-review.ps1` solo acepta `-Alcance last-commit`, así que se corre una vez por squash: para cada `<sha>` de A.1, A.2, A.3, A.5, A.6 y A.7 (leídos con `gh pr view … --json mergeCommit`), con el worktree limpio, `git -C /Users/dn/dev/wt-fase11-lead checkout --detach <sha>`, luego `cross-review.ps1 -Con kimi -Alcance last-commit` con la salida guardada en `R.1/kimi-<fila>.md`, y al final `checkout fase11/revision-r1` (el código es de muse: `-Excluir` no aplica, como dice «Quién» del base). (2) **Re-mutación** de los seis catálogos con base real: cada mutante se siembra a mano en el worktree y se revierte sin commit, con `PYTHONPYCACHEPREFIX=$(mktemp -d) ORBIT_TEST_DSN=… ./.venv/bin/python -m pytest <archivo> -q -p no:cacheprovider` (medido: `-p no:cacheprovider` no apaga `__pycache__` y dio muertes falsas). Todo sobreviviente se cierra con test por un encargo bis a muse **antes** de Q4: rama `fase11/<fila en minúscula>-cierre-r<K>` (p. ej. `fase11/a2-cierre-r1`), compuerta = la común del base más la Q del carril afectado (Q1 para A.5, Q2 para A.6) o, para filas de fases anteriores, `pytest <archivo de tests de esa fila>` con el DSN; archivos = la fila de esa tarea en «Propiedad de archivos» del plan. Entrega `docs/evidencia/repricing-01/R.1/` con catálogo, re-mutación, el APPROVE de kimi y el `APPROVE lead <sha>`. **Desviaciones de la fila R.1, declaradas en el PR**: los sobrevivientes se cierran en un PR bis y no «en el mismo PR», porque R solo puede tocar evidencia; y si el revisor no es kimi, se anota en `R.1/` y en el PR.

**Cierre** — rama `fase11/cierre` · lead: celdas de A.5, A.6 y R.1 a `cc:完了` con SHA y salvedades; E.0b a `cc:完了` o, si no corrió, `cc:TODO — blocked: E.0a sin veredicto`; `docs/CHAT-CONTEXT.md`.

## Archivos por carril

| Carril | Puede tocar | No toca |
|---|---|---|
| **A** | `app/precio/corrida.py`, `app/precio/cuota.py`, `app/spapi/precio_write.py` (**solo** el parámetro `limitador` de `cambiar_precio`; el marcador `pendiente_sonda` intacto), `app/cli.py`, `docs/DEPLOY.md` §cron, `tests/test_precio_corrida.py`, `tests/test_architecture.py`, `docs/evidencia/repricing-01/A.5/mutantes.md` | `migrations/**`, `app/precio/{tipos,reglas,objetivo,ventas,config,goals_write,cobertura,fuentes}.py`, `app/apply.py`, `app/cycle.py`, `app/api_dashboard.py`, `app/notifica.py`, `plans/**` |
| **B** | `app/api_dashboard.py`, `app/ui.py` y sus plantillas, `app/notifica.py` (solo agrega `notifica_precio`), `tests/test_precio_pantalla.py`, `docs/evidencia/repricing-01/A.6/mutantes.md` | `app/precio/**`, `app/spapi/**`, `app/cli.py`, `migrations/**`, `tests/test_architecture.py`, `plans/**` |
| **D** | `app/ledger.py` (solo la rama de `shipping_fee`: mapeo, regla de costo por orden y conteo de descartes), `tests/test_ledger_sync.py`, `docs/evidencia/repricing-01/E.0/**` | `app/precio/**`, `app/spapi/**`, `migrations/**`, `plans/**`; ningún `UPDATE` sobre `ledger_event` |
| **R** | `docs/evidencia/repricing-01/R.1/**` | todo lo demás (las correcciones van por `fase11/<carril>-cierre-r<K>`) |
| **Cierre** | `plans/repricing-01.md` (solo celdas de estado de A.5, A.6, R.1, E.0b), `docs/CHAT-CONTEXT.md` | todo lo demás |

## Cola (compuertas comunes del base, más esto)

- **Q1 · A**: `pytest tests/test_precio_corrida.py tests/test_precio_write.py tests/test_architecture.py tests/test_cli.py tests/test_compose_deploy.py -q -p no:cacheprovider` con el DSN → `N passed` sin `skipped`; `grep -cE 'corrida|cuota' tests/test_architecture.py` → ≥ 2 (hoy `0`; las dos excepciones **por nombre de archivo**); el diff de `app/spapi/precio_write.py` contra `origin/master` solo toca `limitador` y el docstring de `cambiar_precio`: `git diff origin/master...HEAD -- app/spapi/precio_write.py | grep -c '^[-+].*pendiente_sonda'` → `0` y `git diff -U0 origin/master...HEAD -- app/spapi/precio_write.py | grep -c '^@@'` → ≤ 5 hunks, que el lead lee y cita en el PR (residual declarado: el plan da ese archivo a A.3; la salvedad v1.3 de A.3 autoriza que A.5 agregue `limitador`); `grep -c '^10 13 \* \* \*' docs/DEPLOY.md` → `1` y `grep -c '10 13 \* \* \*' tests/test_precio_corrida.py` → ≥ 1 (la pinza sigue el patrón de `LINEA_CRONTAB_VIGILANTE`: constante en el test, `assert … in deploy`, `flock` y el nombre del log). Fallback: diff que toque el marcador → paso 1 del loop.
- **Q2 · B**: `pytest tests/test_precio_pantalla.py tests/test_api_dashboard.py tests/test_spapi_salud.py tests/test_ui.py tests/test_notifica.py -q -p no:cacheprovider` con el DSN → `N passed` sin `skipped`; `git diff --name-only origin/master...HEAD | grep -c '^app/precio/'` → `0`; `grep -c '^def notifica_precio' app/notifica.py` → `1` y `git diff origin/master...HEAD -- app/notifica.py | grep -c '^-def notifica_'` → `0` (los senders existentes intactos).
- **Q3 · D** (si existe): `pytest tests/test_ledger_sync.py -q -p no:cacheprovider` con el DSN → `N passed` sin `skipped`; `ls docs/evidencia/repricing-01/E.0/consultas/*.sql docs/evidencia/repricing-01/E.0/readback.md`; `git diff origin/master...HEAD | grep -ciE '^\+.*(UPDATE +ledger_event|DELETE +FROM +ledger_event|DO UPDATE)'` → `0` (el INSERT vivo es `ON CONFLICT … DO NOTHING`).
- **Q4 · R**: el diff solo toca `docs/evidencia/repricing-01/R.1/`; el documento cita el SHA revisado, el conteo de mutantes por catálogo con `0` sobrevivientes o el PR bis que los cerró, los dos APPROVE y las desviaciones declaradas de R.1.
- **Q5 · Cierre**: la compuerta del ítem de cierre del base con las filas `A.5 A.6 R.1 E.0b` (E.0b cuenta `1` con `cc:完了` o con `cc:TODO — blocked`).

## Atores propios

| Situación | Qué hace el lead |
|---|---|
| E.0a quedó `cc:TODO` en la Fase 10 | El carril D no se lanza; se declara en el progreso y en el PR de cierre; el resto sigue. |
| A entrega la forma del parche sellada o toca más que `limitador` en `precio_write.py` | Paso 1 del loop con encargo de corrección: sellar es A.4, de David. |
| La re-mutación de R.1 deja sobrevivientes | Un encargo `BRIEF-r<K>.md` por carril afectado en `fase11/<carril>-cierre-r<K>`, mergeado antes de Q4; R.1 cita el PR. |
| `ssh goncloud` negado o sin respuesta para el readback de E.0b | `readback.md` queda con `readback_estado: unknown` y el comando al lado; E.0b cierra `cc:TODO — blocked: readback sin acceso de lectura` y el comando literal va al PR de cierre para David. |
| El lead es `kimi` | La cruzada de R.1 sigue siendo `-Con kimi` (es el revisor que el plan nombra) y se anota en `R.1/` que revisor y lead coinciden de host; la auditoría del lead es la re-mutación, no la cruzada. |
| kimi no está disponible para R.1 | Se corre la cruzada con `-Con grok` (revisores del dueño: solo kimi y grok) y se anota en `R.1/` y en el PR quién revisó; no se espera a kimi más de un día. |

## Inventario

Cuatro ramas de trabajo (tres si D no corre) más la de cierre; cinco ítems de cola (cuatro si D no corre); cuatro filas del plan. **Residuales declarados en el PR de este runbook**: la tabla de archivos de A agrega `app/spapi/precio_write.py` (solo `limitador`; lo autoriza la salvedad v1.3 de A.3) y la de B agrega `app/ui.py`, que las filas A.5 y A.6 del plan no listan; la de D usa `app/ledger.py` porque `app/ingest*.py` no existe. La tarea del tracker (`AUTO-07`, obligatoria por el `CLAUDE.md` de Orbit) se marca `In progress` al arrancar y se anota al cierre, como en la Fase 10 (si el host niega la skill, el comando va al PR de cierre). Cero migraciones, cero despliegues, cero escrituras en Amazon ni en producción. Presupuesto: sin tope de rondas (base, regla 14); referencia de la Fase 8, tres rondas por carril de código. **Fuera de alcance**: A.4, D.0, D.1 (cron incluido), E.2, E.3 en adelante, 0.x, B.x, M.x. **Lo que queda listo para David**: con A.4 hecha y D.0 aplicada, D.1 (sombra en producción: instalar el cron, sembrar goals en `shadow`, cinco corridas leídas con el lead). Verificación de este runbook: un lector fresco que ejecuta y un revisor del diff, una ronda (receta `autopilot-runbook`, base «Qué trae un runbook de fase»).
