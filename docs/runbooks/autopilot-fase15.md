# Autopilot de la Fase 15 — CI completa sin siete minutos de espera

Esto lo ejecutas tú, el lead, en autopilot; David no está y no se le pregunta nada. Hereda `docs/runbooks/base-openclaw.md` v1.0 (roles, lanzamiento, dónde vive cada cosa, precondiciones, reglas, compuertas, ventana segura y atores universales) y cita `docs/runbooks/loop-autopilot.md` por número de sección (ver Loop). Plan: filas 15.0 a 15.5 de `Plans.md` en `origin/main` tras Q0; sus filas mandan en la DoD y este documento manda en el método. La fase deja el CI en dos carriles (15.0: clasificador + gate fast en `quality.yml`, reglas 3 y 8 del quality-kit), el runner con inventario y shards (15.1), tres shards aislados en Actions (15.2), una medición que decide si se optimiza (15.3 → 15.4 condicional) y un cierre que demuestra el fast vivo (15.5). No elimina el trigger de `main`, no toca producción (watchdog, preflight), no crea crons, no toca `config`, no crea tokens, ningún repo lee al otro y no hay `ssh` a ningún lado. Versión 1.0, 2026-09-21 UTC.

**Cómo se lanza (slot 13 de la receta).** El dueño dice «claw, empieza la Fase 15» y claw corre `bash scripts/lanzar-fase.sh 15 -- <cli> <flag-sin-preguntas>` con el CLI y el flag del host que elija; el script encuentra esta rama (Q0 pendiente), crea `/Users/dn/dev/wt-f15-lead` y termina en `LISTO fase15-lead /Users/dn/dev/wt-f15-lead` o `ATORADO <razón>`. **Pantalla:** `/runbook/tablero/c/fase15-ci` desde el primer comando.

**Antes de nada: Q0.** Naces parado en la rama de este PR (`docs/fase15-plan-ajustes`, PR #113: los 2 commits del ajuste + este runbook, nada más), porque la DoD que obedeces vive ahí todavía. Tu primer trabajo es mergear Q0 en ventana segura; después `git fetch origin && git checkout --detach origin/main` y sigues. Si Q0 ya está mergeado cuando claw lanza, naces detached en `origin/main` y saltas al Nacimiento. Si este archivo no estuviera en tu worktree, `bash scripts/runbook.sh 15` dice dónde está; la tabla de abajo ES la autorización: lo que no está en ella se rechaza, nunca se pregunta. El 事前確認 del plan lista scope 15.1–15.5 porque 15.0 se agregó en el ajuste; S usa el mismo evento (push `fase15/*`, PR, Actions) por ser fila de la misma fase con un PR por carril por loop §5: lectura de este runbook, declarada en Q1.

**Nacimiento (segunda mitad de Q0, antes de ningún carril).** Ya mergeaste Q0 arriba; ahora naces: primero, si tu sesión se llama `fase15-lead`, renómbrate a `<cli>-wt-f15-lead` (`/opt/homebrew/bin/tmux rename-session -t fase15-lead <cli>-wt-f15-lead`, con tu token de CLI): el lanzador la nombra sin `wt-` y `arranque-de-fase.sh` la busca con `wt-f15-` (solo el nombre; si el cwd está mal, ningún rename lo arregla: claw relanza, base). El 0.0 suma tres comprobaciones: `git -C /Users/dn/quality-kit rev-parse --verify origin/master` resuelve (si no, K queda atorado y lo demás sigue); `/Users/dn/.local/bin/pwsh --version` responde (K lo exige); `/opt/homebrew/bin/gh repo view gon0801/goncloud-openclaw --json name` y lo mismo para `gon0801/quality-kit` responden (si openclaw falla, la fase no arranca: ator universal de GitHub; si solo quality-kit falla, K queda atorado). Primer comando de la fase, con `<host>` = tu token de CLI (el que claw usó al lanzarte: `glm`, `muse`, `cursor-agent`, `claude`, `codex`, `grok`, `kimi`, `dsh` o `zcode`; NO un agentId ni el nombre de sesión), los dos `<ISO>` (`date -u +%Y-%m-%dT%H:%M:%SZ`), el `<sha>` de Q0 (`/opt/homebrew/bin/gh pr view 113 --json mergeCommit --jq '.mergeCommit.oid'`: el merge commit, no la punta de main, que un snapshot pudo mover); después escribe `.saikit/progress/15-sesiones.txt` con `lead - <tu sesión ya renombrada>` y repite `bash scripts/arranque-de-fase.sh 15`: todo VERDE salvo `vigilantes` cierra Q0.

```
cat > .saikit/progress/15.json <<'JSON'
{"schema":"runbook-progress.v1","runbook":"docs/runbooks/autopilot-fase15.md","fase":"15","corrida":"fase15-ci","proyecto":"openclaw","titulo":"Autopilot de la Fase 15 — CI completa sin siete minutos de espera","plan":{"repo":"gon0801/goncloud-openclaw","ruta":"Plans.md","seccion":"Fase 15"},"lead":{"agente":"<host>","inicio":"<ISO>","actualizado":"<ISO>"},"atencion_requerida":{"necesaria":false,"motivo":null,"desde":null},"siguiente_paso":"Q0 mergeado; arrancan S, K y R en paralelo, luego W.","carriles":[{"id":"S","nombre":"Seleccion","repo":"gon0801/goncloud-openclaw","rama":"fase15/seleccion","tareas":["15.0"],"estado":"pendiente","paso_loop":0,"pr":null,"head":null,"approve_lead":null,"ci":"pendiente","coderabbit":"pendiente","residuales":[],"detenido_por":null},{"id":"K","nombre":"Kit","repo":"gon0801/quality-kit","rama":"fase15/reglas","tareas":["15.0"],"estado":"pendiente","paso_loop":0,"pr":null,"head":null,"approve_lead":null,"ci":"pendiente","coderabbit":"pendiente","residuales":[],"detenido_por":null},{"id":"R","nombre":"Runner","repo":"gon0801/goncloud-openclaw","rama":"fase15/runner","tareas":["15.1"],"estado":"pendiente","paso_loop":0,"pr":null,"head":null,"approve_lead":null,"ci":"pendiente","coderabbit":"pendiente","residuales":[],"detenido_por":null},{"id":"W","nombre":"Shards","repo":"gon0801/goncloud-openclaw","rama":"fase15/shards","tareas":["15.2"],"estado":"pendiente","paso_loop":0,"pr":null,"head":null,"approve_lead":null,"ci":"pendiente","coderabbit":"pendiente","residuales":[],"detenido_por":null},{"id":"O","nombre":"Optimizacion","repo":"gon0801/goncloud-openclaw","rama":"fase15/optimizacion","tareas":["15.4"],"estado":"pendiente","paso_loop":0,"pr":null,"head":null,"approve_lead":null,"ci":"pendiente","coderabbit":"pendiente","residuales":[],"detenido_por":null}],"cola":[{"id":"Q0","prs":[{"repo":"gon0801/goncloud-openclaw","pr":113}],"estado":"verificado","ventana":null,"merge_commits":["<sha>"],"verificado":"ok","detenido_por":null,"avance":100},{"id":"Q1","prs":[],"estado":"pendiente","ventana":null,"merge_commits":[],"verificado":null,"detenido_por":null,"avance":0},{"id":"Q2","prs":[],"estado":"pendiente","ventana":null,"merge_commits":[],"verificado":null,"detenido_por":null,"avance":0},{"id":"Q3","prs":[],"estado":"pendiente","ventana":null,"merge_commits":[],"verificado":null,"detenido_por":null,"avance":0},{"id":"Q4","prs":[],"estado":"pendiente","ventana":null,"merge_commits":[],"verificado":null,"detenido_por":null,"avance":0},{"id":"Q5","prs":[],"estado":"pendiente","ventana":null,"merge_commits":[],"verificado":null,"detenido_por":null,"avance":0}],"eventos":[],"cierre":{"at":null,"telegram_message_id":null,"resumen":null}}
JSON
~/.openclaw/bin/openclaw gateway call runbook.progress.set --params "$(cat .saikit/progress/15.json)" --timeout 30000
```

## Preaprobaciones del dueño

| Operación | Alcance | Decisión |
|---|---|---|
| Crear worktrees y ramas `fase15/*` | `/Users/dn/dev/wt-f15-*` en openclaw; `/Users/dn/dev/wt-f15-K` vía `git -C /Users/dn/quality-kit` | Aprobado |
| Lanzar implementadores en tmux con su flag sin preguntas | los cinco carriles | Aprobado |
| `gh pr create`, `gh pr comment`, `gh pr ready`, `gh api` de lectura, `gh run list\|view` | `gon0801/goncloud-openclaw` y `gon0801/quality-kit` | Aprobado |
| Revisión cruzada por `cross-review.ps1` | cualquier revisor del conjunto | Aprobado |
| Mergear por la ruta del kit, en ventana segura | los ítems de openclaw | Aprobado |
| Mergear con `gh pr merge --squash`, sin ventana | solo `fase15/reglas` en quality-kit (no despliega nada) | Aprobado |
| `runbook.progress.set` y `runbook.progress.get` | la corrida `fase15-ci` y lecturas de cualquier otra | Aprobado |
| `install-ai-rules.ps1` con los 4 paths a temporales | solo con `-ClaudeMdPath/-CodexAgentsPath/-KimiAgentsPath/-KiloAgentsPath` bajo `mktemp -d`; jamás sin parámetros | Aprobado |
| `init-repo.ps1 -RepoPath` | solo contra repos temporales de `tests/temp-fixtures` | Aprobado |
| `cron list` de solo lectura | cualquier cron (ventana segura, CHAT) | Aprobado |
| Borrar ramas y worktrees de la fase al cierre | `fase15/*` en openclaw, `fase15/reglas` en quality-kit, `/Users/dn/dev/wt-f15-*` | Aprobado |
| Un Telegram de cierre a David | forma `CERRADA` de `docs/spec/seguimiento.v1.md`, `--silent`, una sola vez | Aprobado |
| `install-ai-rules.ps1` sin parámetros, o con un path real | cualquiera | **Negado** — escribe en `~/.claude`, `~/.codex`, `~/.kimi-code`, `~/.config/kilo` reales |
| Editar entre marcadores `QUALITY-KIT` | cualquiera | **Negado** — se regenera con el instalador |
| Crear `AGENTS.md` en openclaw | cualquiera | **Negado** — el plan lo prohíbe; no existe en el baseline |
| Pushes rojos deliberados («probar» CI) | cualquiera | **Negado** — el plan manda fixtures locales |
| Quitar el trigger de `main`, tocar watchdog/preflight de producción | cualquiera | **Negado** — fuera de alcance del plan |
| `cron add\|edit\|rm`, `config patch` | cualquiera | **Negado** — el plan no trae reloj ni cambios al gateway |
| `ssh` a cualquier lado, leer secretos | cualquiera | **Negado** |

## Seguimiento

El lead lanza cada sesión con `corrida.sh lanzar-sesion`, que la marca antes del
primer texto. Claw vigila las sesiones marcadas y recuerda su silencio cada 30 minutos;
ese aviso es interno y no crea un cron de la fase. En cada cambio de
estado, el lead actualiza el tablero por RPC del gateway, como define
`loop-autopilot.md` §8. El único canal a David es el Telegram de cierre que ya
lee el cron `verif-sync-repos`; no hay avisos intermedios al dueño.

## Clases de comando

| Clase | Para qué | Candado |
|---|---|---|
| `gh` | PRs, CI, ventana y merges autorizados | Permitido por las filas de Preaprobaciones |
| `ssh` | Ninguna operación | Negado para esta fase |

## Prohibido

Preguntarle algo a David antes del cierre, salvo la única fila que lo permite (base, «la reversa que no entra»). Usar `--no-verify`. Mergear por cualquier ruta que no sea la de su tabla (kit en openclaw, squash directo solo en K). Correr instaladores del kit contra rutas reales. Pushear rojo a propósito. Tocar producción del watchdog o del preflight. Quitar el trigger de `main` o saltar batería por tipo de cambio fuera del clasificador. Crear `AGENTS.md` en openclaw. Editar entre marcadores. Dar de alta, editar o borrar crons. Mandar a David otro Telegram que no sea el de cierre. Leer o pegar secretos, tokens o lanzadores de `~/bin`.

## Loop

Cada tarea corre loop §3 entero (encargo en archivo, TDD con rojo pegado, auditoría del lead con mutación propia, PR draft, §4 con revisor rotado y `-Excluir` del implementador, promoción, CodeRabbit leído, `APPROVE lead <sha>`, merge §6 con base al día por merge, §8 en cada cambio); PRs por §5, merges por §6 en la ventana segura del base, reanudación por §9 y base, revisión de cierre por §10. Cinco desviaciones declaradas: (a) `arranque-de-fase.sh 15` queda ROJO solo en `vigilantes`, por diseño (Negado arriba; la fase no crea empuje-15 ni usa reloj global, así que `avance-tareas` ausente también es por diseño; el seguimiento es solo §8 en cada cambio); (b) W arranca cuando S y R mergean; K no lo bloquea (repos disjuntos: W no lee generadores del kit); (c) en quality-kit no hay kit de merge (medido: sin `.saikit` ni hooks): K sustituye §6 por `/opt/homebrew/bin/gh pr comment <n> --repo gon0801/quality-kit --body 'APPROVE lead <sha>'` + `/opt/homebrew/bin/gh pr merge <n> --repo gon0801/quality-kit --squash`, sin ventana porque no despliega; (d) O nace `pendiente` y 15.3 lo activa (implementando + ítem `15.4` nuevo en la cola) o lo omite (carril `omitido` con `detenido_por`, fila `cc:OMITIDA`); (e) 15.3 y el cierre los hace el lead (fast/skip, sin implementador).

## Carriles

**Rama base: `origin/main` fresco con Q0 dentro**, para S y R, que arrancan en paralelo con K (archivos disjuntos; worktrees `/Users/dn/dev/wt-f15-S|K|R`, sesiones `<token>-wt-f15-<LETRA>` con la letra del carril). K nace de `origin/master` de quality-kit. W arranca cuando S y R mergean, comprobado con `git fetch origin && git merge-base --is-ancestor <sha-Q1> origin/main && git merge-base --is-ancestor <sha-Q3> origin/main` (los shas salen de `merge_commits` de Q1/Q3 en el progreso); su rama nace de `origin/main` con los dos dentro. O arranca solo si 15.3 la activa (de `origin/main` con W dentro); el cierre nace de `origin/main` con todo dentro. El orden `si no` de la tabla es de lanzamiento (disponibilidad y cuota al lanzar, como claw); una vez corriendo, la cuota detiene el carril sin cambiar de proveedor (base). La DoD de cada tarea es su celda en `Plans.md`, verbatim.

| Carril | Rama | Tareas | Quién |
|---|---|---|---|
| **S · Selección** | `fase15/seleccion` | 15.0 (openclaw) | `glm`, si no `cursor-agent`, si no `muse` |
| **K · Kit** | `fase15/reglas` en quality-kit (`master`): `git -C /Users/dn/quality-kit worktree add /Users/dn/dev/wt-f15-K -b fase15/reglas origin/master` | 15.0 (kit) | `muse`, si no `cursor-agent`, si no `glm` |
| **R · Runner** | `fase15/runner` | 15.1 | `cursor-agent`, si no `glm`, si no `muse` |
| **W · Shards** | `fase15/shards`, tras Q1+Q3 (`/Users/dn/dev/wt-f15-W`) | 15.2 | `glm`, si no `muse`, si no `cursor-agent` |
| **O · Optimización** | `fase15/optimizacion`, solo si 15.3 la activa (`/Users/dn/dev/wt-f15-O`) | 15.4 | el mismo implementador de R, si no el siguiente de su orden |

Lo que un usuario vería: S, PRs de solo docs que cierran sin pagar 7 minutos de CI; R y W, nada (CI interno más rápido); K, nada (scripts del kit); O, nada (tests más rápidos); el cierre, la evidencia. Esta fase no trae agente `usuario`: es CI interno y se demuestra con `gh run list` (ver Medición), no con una pantalla.

## Archivos por carril

| Carril | Puede tocar | No toca |
|---|---|---|
| **S** | `scripts/clasificar-cambio.sh`, `scripts/ci-fast-allowlist.txt`, `.github/workflows/quality.yml`, `scripts/tests/test-clasificador-cambio.sh`, `scripts/tests/fixtures/clasificador/**`, `.saikit/scratch/S/**` | `scripts/run-checks.sh`, `scripts/tests/test-summa-gate-quality-entrypoints.sh`, `Plans.md`, `AGENTS.md` (no crearlo), `summa-gate/**`, `tablero-runbook/**` |
| **K** | en quality-kit: `install-ai-rules.ps1`, `init-repo.ps1`, `templates/**`, `tests/**` | en quality-kit: `cross-review.ps1` y todo lo demás; todo este repo; rutas reales de reglas globales |
| **R** | `scripts/run-checks.sh`, `scripts/tests/test-tmux-activity-watch.sh` (solo retirar la llamada anidada), `scripts/tests/test-runner-shards.sh`, `scripts/tests/fixtures/runner-shards/**`, `.saikit/scratch/R/**` | `scripts/tests/test-mac-tmux-control.sh` (intacto: diff vacío), `.github/workflows/quality.yml`, `Plans.md`, producción (`scripts/mac/**`, `corrida/**`) |
| **W** | `.github/workflows/quality.yml`, `scripts/tests/test-summa-gate-quality-entrypoints.sh`, `scripts/tests/fixtures/quality-shards/**`, `.saikit/scratch/W/**` | `scripts/run-checks.sh`, `scripts/clasificar-cambio.sh`, `scripts/ci-fast-allowlist.txt`, `Plans.md` |
| **O** | `scripts/tests/**`, `.saikit/scratch/O/**` | producción: `scripts/mac/tmux-activity-watch.sh`, `corrida/preflight.sh`, `scripts/run-checks.sh`, `.github/workflows/quality.yml`, `Plans.md` |
| **cierre** | `Plans.md` (solo celdas Status), `.saikit/progress/15.json`, `.saikit/progress/15-sesiones.txt`, `docs/evidence/fase15-ci-performance.md`, `.saikit/scratch/lead/**` | todo lo demás |

## Cola de merge

Cada ítem pasa la compuerta común del base (CI de su SHA, log solo con commits del carril + merges de regla 5, PR fuera de borrador) y va en ventana segura, salvo K (sin ventana: no despliega): mergear aquí **es** desplegar (`merge_despliega: publica`). 15.3 y 15.5 no tienen carril: las cubre Q5, el renglón «Cierre» que cuenta sin carril. Nunca hay más de tres PRs abiertos: S, K y R abren en paralelo; W abre cuando S y R cierran; O abre tras la decisión 15.3; el cierre abre al final. Una corrección tras el merge que no sea reversa va en `fase15/seguimiento-<carril>` desde `origin/main` (worktree `/Users/dn/dev/wt-f15-seg-<carril>`), loop reducido (CI + CodeRabbit + `APPROVE`), en ventana segura, antes del cierre, y respeta el tope de tres.

| # | Ítem | Compuerta propia | Fallback |
|---|---|---|---|
| Q0 | Plan ajustado + runbook (`docs/fase15-plan-ajustes`, PR #113) | Loop reducido (CI + CodeRabbit leído + `APPROVE` + kit, sin cruzada: el plan es del dueño y el runbook llega verificado); `git show origin/main:Plans.md \| grep -c 'Política de selección aprobada'` → `1`, `git show origin/main:Plans.md \| grep -c '^| 15.0 |'` → `1`, `git cat-file -e origin/main:docs/runbooks/autopilot-fase15.md`, y `bash scripts/arranque-de-fase.sh 15` en VERDE salvo `vigilantes` | Sin Q0 la fase no arranca: sin la DoD nueva no hay qué obedecer |
| Q1 | S · selección (15.0 openclaw) | En el worktree de S (los scripts hacen `cd` a su raíz solos): `bash scripts/tests/test-clasificador-cambio.sh` y `bash scripts/run-checks.sh` en verde sobre el head, con el rojo primero pegado; los fixtures cubren docs permitidos, código, mezcla, borrados, renombres, operativos y comparación fallida (el lead los lee); el gate acepta shards omitidos solo con clasificación fast válida (fallo, cancelación o clasificación ausente nunca pasan); la allowlist clasifica fast esos 4 paths del cierre (`Plans.md`, `docs/evidence/fase15-ci-performance.md`, `.saikit/progress/15.json`, `.saikit/progress/15-sesiones.txt`), con la interfaz que S documenta en su PR; `git cat-file -e HEAD:AGENTS.md` falla (no se creó); lectura del scope 15.0 declarada en el PR | Vuelve a loop §3 con el log como encargo |
| Q2 | K · kit (15.0 quality-kit) | En `wt-f15-K`: `/Users/dn/.local/bin/pwsh -NoProfile -File ./tests/validate-syntax.ps1` y `/Users/dn/.local/bin/pwsh -NoProfile -File ./tests/run-tests.ps1` en verde; `<n>` sale de `/opt/homebrew/bin/gh pr list --head fase15/reglas --repo gon0801/quality-kit --json number --jq '.[0].number'` y `/opt/homebrew/bin/gh pr checks <n> --repo gon0801/quality-kit` queda verde en windows + macos; regeneración contra temporales (`-ClaudeMdPath/-CodexAgentsPath/-KimiAgentsPath/-KiloAgentsPath` a `mktemp -d`, `-RepoPath` a repo temporal) pegada en el PR con las reglas 3 y 8 nuevas y sin edición entre marcadores; se sella aparte (otro repo, sin kit: comentario + merge squash directo de Loop (c)) | Loop §3 en ese repo, mismo implementador |
| Q3 | R · runner (15.1) | En el worktree de R: `bash scripts/tests/test-runner-shards.sh` y `bash scripts/run-checks.sh` en verde sobre el head, con el rojo primero pegado; `SAIKIT_SHARD=4/3 bash scripts/run-checks.sh` y `SAIKIT_SHARD=x bash scripts/run-checks.sh` salen ≠0; una prueba roja registra una sola ejecución (el lead lo lee en la salida de `run-checks.sh`: un solo registro por prueba); `git diff origin/main...HEAD --stat -- scripts/tests/test-mac-tmux-control.sh` vacío | Vuelve a loop §3 con el log como encargo |
| Q4 | W · shards (15.2) | Fixtures del workflow + `test-summa-gate-quality-entrypoints.sh` en verde; `grep -n 'fail-fast: false' .github/workflows/quality.yml` sobre el head; `<n>` sale de `/opt/homebrew/bin/gh pr list --head fase15/shards --json number --jq '.[0].number'`, `HS` de `/opt/homebrew/bin/gh pr view <n> --json headRefOid --jq .headRefOid`, `ID` de `/opt/homebrew/bin/gh run list --repo gon0801/goncloud-openclaw --branch fase15/shards --workflow Quality --limit 50 --json databaseId,headSha --jq '.[] \| [.databaseId, .headSha] \| @tsv'`, eligiendo el renglón de `HS`; CI de `ID` con los 3 shards + gate en success (`/opt/homebrew/bin/gh api repos/gon0801/goncloud-openclaw/actions/runs/$ID/jobs --paginate --jq '.jobs[] \| [.name, .conclusion] \| @tsv'`); la unión de las listas que publica cada shard en `/opt/homebrew/bin/gh run view $ID --log` nombra el inventario exactamente una vez: glob `scripts/tests/*.sh` en el head + entradas Node/sintaxis/corpus que Q3 fijó en su PR, sin duplicados (el lead compara); ningún run de la rama en `failure` salvo infra declarada en el PR (sin pushes rojos deliberados) | Vuelve a loop §3 con el log como encargo |
| 15.4 | O · optimización (solo si 15.3 la activa; si no, no hay ítem) | Cada sustitución conserva sus assertions y demuestra con un defecto sembrado localmente que sigue detectando la regresión; `git diff origin/main...HEAD --stat -- scripts/mac/ corrida/ scripts/run-checks.sh .github/workflows/` vacío (cero producción); solo se midieron los tests afectados + una validación del SHA final en CI | Si requiere cambiar producción: propuesta aparte como residual, O se cierra sin ese cambio; si no, loop §3 |
| Q5 | cierre (`fase15/cierre`, `/Users/dn/dev/wt-f15-cierre`, 15.3+15.5) | Revisión §10 contra la DoD literal; PR con el loop reducido del base: la evidencia, las celdas (`cc:完了` con PR+merge; 15.4 `cc:完了` o `cc:OMITIDA — <razón de 15.3>`), `15.json` y `15-sesiones.txt`; evidencia de integración 15.0 enlazada en el cuerpo (regeneración de Q2 + este PR fast); CI del PR clasifica fast (batería omitida por clasificación, checks documentales verdes, sin `Install openclaw`: `/opt/homebrew/bin/gh api repos/gon0801/goncloud-openclaw/actions/runs/$ID/jobs --paginate --jq '.jobs[] \| [.name, .conclusion] \| @tsv'` con los nombres de job que Q1 fijó en su PR, `ID` como en Q4); objetivo de 4 min declarado logrado/no con causa en la evidencia y en el cuerpo del PR; tras merge: ramas `fase15/*`, `docs/fase15-plan-ajustes` y `fase15/reglas` borradas (`push origin --delete`, `branch -D`, `worktree remove`), sesiones desmarcadas, `bash scripts/cierre-de-fase.sh 15` en VERDE con salida 0; un Telegram `CERRADA` (`cierre.telegram_message_id` en el progreso), lo último tras el VERDE | `cierre-de-fase.sh` es un bucle: su lista en ROJO es la lista de lo que falta |

## Medición (15.3, la hace el lead tras Q4)

Sobre la corrida completa del PR de W: `HS=$(/opt/homebrew/bin/gh pr view <n> --json headRefOid --jq .headRefOid)` (el head cuyo CI verde autorizó el merge; el squash de main es otra corrida y no se mide); `/opt/homebrew/bin/gh run list --repo gon0801/goncloud-openclaw --branch fase15/shards --workflow Quality --limit 50 --json databaseId,headSha,conclusion,createdAt,updatedAt,displayTitle --jq '.[] | [.databaseId, .headSha, .conclusion] | @tsv'`, elige el renglón de `HS` y fija `ID` con ese número; `/opt/homebrew/bin/gh api repos/gon0801/goncloud-openclaw/actions/runs/$ID/jobs --paginate --jq '.jobs[] | [.name, .conclusion, (.started_at // "?"), (.completed_at // "?")] | @tsv'`; instalación vs ejecución salen de `/opt/homebrew/bin/gh api repos/gon0801/goncloud-openclaw/actions/runs/$ID/jobs --paginate --jq '.jobs[] | .name as $j | .steps[]? | [$j, .name, .conclusion] | @tsv'` (`setup-*` e `Install*` son instalación; `createdAt→started_at` es cola de infra). Baseline: el plan cita 412 s (duración del job `quality` del run 35547004899); re-medido con esta definición (primer job iniciado → gate terminado) son 422 s: manda 422 s y se anotan ambos. Objetivo: <240 s con la misma definición. Tres salidas, las del plan: cumple → O se omite con razón; supera por infra → salvedad sin reruns; supera por esperas artificiales en tests → se activa O. Escribe `.saikit/scratch/lead/15.3-medicion.md` (URL, SHA, tiempos por shard, cobertura, cuello de botella, decisión) y resuelve el carril O en el progreso; la evidencia final la junta Q5. El cierre fast de Q5 también se mide (<60 s de ejecución, separando cola; no bloquea).

## Cuando algo se atora (propias de esta fase)

| Situación | Qué hace el lead |
|---|---|
| `arranque-de-fase.sh 15` ROJO en `vigilantes` | Esperado (Negado arriba): se sigue. ROJO en otra línea: se arregla; sin VERDE ahí no hay arranque |
| `gh` no ve openclaw / no ve quality-kit | Sin openclaw la fase no arranca (ator universal). Sin solo quality-kit: K atorado con ese `detenido_por` y lo demás sigue |
| `origin/master` de quality-kit no resuelve, o no hay `pwsh` | K atorado con ese `detenido_por` y lo demás sigue |
| S creó `AGENTS.md`, o tocó un archivo fuera de su tabla | Se descarta antes del push con un commit propio (ator universal); si ya pusheó, vuelve a loop §3 |
| El instalador se corrió contra rutas reales | Por cada global tocado: `cp <destino>.bak-<stamp> <destino>` (el instalador deja el backup junto al destino y dice su ruta); se declara en el PR y en el progreso, K vuelve a loop §3 |
| CI de K rojo en un OS | Loop §3 en ese repo con el log como encargo; openclaw no espera: Q1/Q3/Q4 siguen |
| La unión de shards no cubre el inventario exactamente una vez | W vuelve a loop §3 con la comparación como encargo |
| Un implementador propone pushear rojo «para probar» | Negado: fixtures locales (plan 15.2). Se rechaza, nunca se pregunta |
| 15.3 supera 240 s por infraestructura | Salvedad en la evidencia, sin reruns; O se omite y 15.4 cierra `cc:OMITIDA` |
| 15.3 encuentra esperas artificiales en tests | Se activa O (implementando + ítem `15.4` en la cola con avance 0) |
| O necesita cambiar producción | Propuesta aparte como residual en el PR de cierre; no se amplía esta fase |
| W nació antes de que S y R mergeen | Se borra su worktree y rama y se recrea desde `origin/main` con los dos dentro; los drafts pre-base no se rescatan |
| `cierre-de-fase.sh 15` ROJO en `reloj` con «rancio» | Q5 queda `atorado` con ese `detenido_por` y `atencion_requerida: true` (es el único caso que sube a David en esta fase); la fase no creó ni usó el reloj (Negado arriba), así que no lo retira: decide el dueño. Todo lo demás queda cerrado y verificado |
| `cierre-de-fase.sh 15` ROJO en ramas/worktrees | Por cada rama viva `<r>` (las lista el propio script): `git push origin --delete <r>`, `git branch -D <r>` (fueron squash: `-d` se niega), `git worktree remove <path>` (el path lo dice `git worktree list`), y se repite; igual para `fase15/reglas` con `git -C /Users/dn/quality-kit`. La rama Q0 (`docs/fase15-plan-ajustes`) SÍ se borra: matchea `fase15-` del script y su contenido ya está en main |
| `wt-f15-*` trae cambios sin commitear al cerrar | No se toca: `ATORADO worktree sucio` + se declara; decide el dueño |
| `message send` del cierre falla | Se reintenta dos veces; si no sale, se cierra con `telegram_message_id: null` y el error declarado en el PR y el progreso (`CHAT` vacío cae aquí mismo: sin destino no hay envío) |

## Inventario

**Cuentas:** 6 tareas, 5 carriles + cierre, 6 ítems de cola (+`15.4` si se activa), 2 repos. **Presupuesto:** tres sesiones de implementador en paralelo y luego W y O; si un proveedor se queda sin cuota, ese carril se detiene y los demás siguen (base). **Fuera de alcance:** todo el Reject del plan (borrar cobertura, bajar timeouts vivos, tolerar tests fallidos, clasificar solo por extensión, repetir CI por un tiempo, quitar el trigger de `main`, tocar producción). **Canal a David:** un solo Telegram de cierre, forma `CERRADA` de seguimiento.v1. `CHAT` (vale `6470689715` hoy, pero se lee, no se pega): `CHAT=$(~/.openclaw/bin/openclaw cron list --json 2>/dev/null | python3 -c "import sys,json; t=sys.stdin.read(); d=json.loads(t[t.index('{'):]); print(next(((j.get('delivery') or {}).get('to') or '' for j in d.get('jobs',[]) if j.get('name')=='verif-sync-repos'),''))")` y `test -n "$CHAT"`; `M=$(mktemp); cat > "$M" <<'MSG'` + las 4 líneas de seguimiento.v1 (`[CERRADA] Fase de velocidad de la revisión automática terminada` / `Que cambio: <tarda <N> minutos|no llegó a 4 minutos por <causa en palabras simples>>` / `Que sigue: <nada pendiente|la optimización pendiente en palabras simples>` / `Que necesito de ti: nada`) + `MSG`; envío `~/.openclaw/bin/openclaw message send --channel telegram -t "$CHAT" --silent --json -m "$(cat "$M")"`; al progreso solo el valor del campo `messageId` del `--json`, en `cierre.telegram_message_id`. **Lo que queda listo para David:** la CI completa tarda menos de 4 minutos y los cambios de solo docs ya no la pagan. Pantalla: `/runbook/tablero/c/fase15-ci`.
