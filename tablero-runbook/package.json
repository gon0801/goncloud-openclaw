{
  "name": "tablero-runbook",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "node --check lib.ts && node --check index.ts && node --check github.ts && node --check contrato.ts && node --check render.ts && node --check plan.ts && node --check live-bus.ts && node --check seguimiento.ts && node --check seguimiento-render.ts",
    "test": "node --test"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
