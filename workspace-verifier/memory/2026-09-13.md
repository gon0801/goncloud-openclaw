# 2026-09-13 — memoria del verifier (Pesa)

## Entorno del nodo Mac (David's MacBook Pro) — gotchas duraderos

- En el nodo Mac, `bash` del PATH de exec es /bin/bash 3.2.57 y NO parsea hooks/summonaikit-harness.sh (syntax error en heredoc ~línea 1637). Todo lo que corra el hook o la batería necesita bash 5.x: usar `/opt/homebrew/bin/bash` o `PATH=/opt/homebrew/bin:/usr/bin:/bin`. <!-- project: github.com/gon0801/goncloud-openclaw -->
- `tools/golden-harness.sh` usa `sha256sum` (GNU coreutils). En la Mac solo existe `gsha256sum`/`shasum`; sin GNU `sha256sum` en PATH, los sha salen vacíos y el golden --check falla con ".dedup/: Is a directory" y un falso "ya no se comporta como la baseline". Fix local: symlink `sha256sum -> /opt/homebrew/bin/gsha256sum` en un dir de PATH. <!-- project: github.com/gon0801/goncloud-openclaw -->
- La conexión exec al nodo se corta a mitad de comandos largos y mata los procesos hijos (una corrida de la batería de mutaciones murió sin EXIT marker). Correr suites largas detached: `nohup ... >log 2>&1 & echo PID=$!` con `touch <log>.done; wait`, y hacer polling del log. <!-- project: github.com/gon0801/goncloud-openclaw -->
- `tools/saikit-decision.sh --check` exige `--task <id>`; sin él devuelve exit 2 "falta --task <id>". <!-- project: github.com/gon0801/goncloud-openclaw -->

## Verificación Ronda 2 Bloque 8 (PR #318, diff 09a8c78..d05cedd) — resultado y hallazgo

- Clon de lectura usado: `/Users/dn/dev/verify-b8-r2` (worktree detach en d05cedd9). Master 30b3418 y el wt del implementador `~/summonaikit-claude-wt-bloque8` NO se tocan. Evidencia larga en `.saikit/scratch/bloque8-verify-r2/` del clon propio; blast en `.saikit/findings/blast-21.4r2-verify.json`. <!-- project: github.com/gon0801/goncloud-openclaw -->
- Veredicto ronda 2: puntos 1,2,3,4,5,6,8 VERIFICADOS (trail físico hash-object + GIT_NO_REPLACE_OBJECTS=1 con casos assume_unchanged/replace_blob; anclaje a veredicto sellado con --git-common-dir y guard -L; 177/177 mutantes corridos por mí con los 2 nuevos trail_raiz_sin_hash_disco y trail_raiz_sin_veredicto_sellado; golden --check exit 0; docs/PR/rastro OK; sin rewrite y scope limpio). <!-- project: github.com/gon0801/goncloud-openclaw -->
- ÚNICO hallazgo (punto 7): CI de #318 rojo en d05cedd — `suite (7/7)` y `gate` FAIL por `test_golden_provenance`: `tests/golden/baseline.txt` declara `# hook_sha256: ef8aa2a5…` (grabado en e4f887a para hook 8699286/21.4r1) pero el sha real del hook en d05cedd es `3e2525416…`. Causa: commit 9bdcba5 (21.4r2) cambió el hook y NO re-grabó la baseline. El comportamiento no deriva (golden --check pasa); solo el candado de identidad está rojo. Fix del implementer: re-grabar baseline con `tools/golden-harness.sh --record` contra fuente actual, diff debe ser solo la cabecera, copiar a tests/golden/baseline.txt y pushear. <!-- project: github.com/gon0801/goncloud-openclaw -->
- Patrón útil de diagnóstico: reproducir manualmente el paso que falla (p.ej. invocar el hook a mano con el payload de lab_init y ver .out/.err) antes de reintentar; la primera pasada de golden "fallando" era el entorno (sha256sum), no drift. <!-- project: github.com/gon0801/goncloud-openclaw -->
